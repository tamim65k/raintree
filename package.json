{
    "name": "ip-terminal-app",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev:client": "cd client && npm run dev",
        "dev": "npm run dev:client",
        "build": "cd client && npm install && npm run build",
        "vercel-build": "npm run build",
        "start": "cd client && npm run preview"
    },
    "dependencies": {
        "concurrently": "^8.2.0",
        "express": "^4.18.2",
        "cors": "^2.8.5"
    }
}